<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl"><span class="font-light"></span> Hello, <%= user.name %> 👋</h3>
        <h5>You can create a new post.</h5>
        <form class="flex flex-wrap mb-5" action="/post" method="post">
            <textarea placeholder="What's on your mind?" class="p-3 w-1/3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
            <input class="text-m mx-3 w-40 px-3 py-2 bg-blue-500" type="submit" value="Create new Post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-xl text-zinc-300">Your posts</h3>
            <div class="postcontainer mt-4">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="mb-3 post w-1/3 p-4 border-2 border-zinc-800 rounded-md">
                        <h4 class="text-blue-400 mb-2"><%= post.user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>
                        <div class="btns flex gap-3 mt-1">
                            <a class="text-blue-500 " href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-yellow-500 " href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>